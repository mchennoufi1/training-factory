{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit User</h1>

    {{ form_start(form) }}
        {{ form_widget(form.email) }}
        {{ form_widget(form.password) }}
        <button type="submit">Save</button>
    {{ form_end(form) }}
{% endblock %}
